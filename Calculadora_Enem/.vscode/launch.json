{

    "version": "0.2.0",
    "configurations": [    
      {
"type": "pwa-chrome",
"request": "launch",
"name": "launch chrome against localhost",
"url": "http://localhost:5500",
"webroot": "${workspaceFolder}"
    }

    ]
}